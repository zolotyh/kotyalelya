{
  "name": "lelya",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel run index.html",
    "build": "rm -r ./docs && parcel build index.html --public-url ./ --out-dir ./docs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "gh-pages --dist ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^1.12.4",
    "qrcode": "^1.4.4"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "sass": "^1.26.10"
  }
}
